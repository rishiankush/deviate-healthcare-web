import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Daviate Healthcare - Empower Your Health",
  description: "Leading pharmaceutical manufacturer specializing in nutraceuticals, health supplements, and third-party manufacturing. ISO 9001:2015 & WHO-GMP certified.",
  keywords: "pharmaceutical, healthcare, nutraceuticals, third party manufacturing, WHO-GMP, ISO certified, Baddi",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
